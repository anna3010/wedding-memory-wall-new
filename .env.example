# Supabase Configuration for Cloudflare Pages
# Get these values from your Supabase Dashboard → Project Settings → API

# Public Supabase URL (for client-side)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Public Supabase Anon Key (for client-side)
# This key is safe to expose to the client
PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here

# Note: For Cloudflare Pages, add these in Pages Settings → Environment Variables
# Only PUBLIC_ prefixed variables are needed for client-side Supabase operations
